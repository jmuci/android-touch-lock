# android-touch-lock
Touch Lock is a lightweight Android utility app that temporarily disables touch input on the screen while keeping content visible.  It is designed for supervised scenarios, such as:  Letting a toddler watch a video without accidentally tapping.
